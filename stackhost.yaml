version: "2.0.0"

user:
  name: itzzzyashu
  github: itzzzyashu

runtime:
  image: python:3.11-slim

commands:
  package: "nodejs"
  build:
    - pip install flask
  start: npm install && node app.js

repository:
  branch: main
  auto_deploy: true
